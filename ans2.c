#include <stdio.h>
#include <math.h>

int main(void) {
	int size;
	int c;
	char a[];
	string name;
	string result[];
	
	for (i=0,i<size,i++){
	    if int(a[i]) > int(a[i+1])
	       { c=a[i];
	         a[i]=a[i+1];
	         a[i+1]=c;
	           
	           
	           
	    }
	    char b[];
	    
	    
	    for(j=0,j<(size)!,j++)
	    { for(k=0,k<(size-j)!,k++)
	        {for(s=j,s<(size-j),s++){
	            if(a[s]>a[s+1]){
	                c=a[s]
	            }
	        }
	        
	        
	        
	        }
	        
	        b[j]=a[j];
	        
	        
	    }
	    
	    
	
	
	
	
	
	
	return 0;
}

